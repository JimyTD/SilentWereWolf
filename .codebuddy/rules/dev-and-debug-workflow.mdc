---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-28T07:24:13.203Z
provider: 
---

# 开发与调试工作流规范

## 核心原则
**改完代码必须验证，不能改了就说"完成了"。**

## 开发流程

### 1. 修改前
- **先读后改**：修改文件前必须先读取文件内容，确认当前状态
- **理解上下文**：了解修改会影响哪些模块
- **说明方案**：复杂修改前先向用户说明计划

### 2. 修改时
- **最小改动**：使用 `replace_in_file` 做精确替换，不整文件重写
- **保持一致性**：遵循项目已有的代码风格和命名规范
- **不留调试代码**：完成调试后清理 `console.log`、临时注释等

### 3. 修改后（验证清单）

#### 前端修改后
```bash
# 1. 检查 lint 错误
cd i:\AIGameTest\SilentWerewolf\client && npx vite build 2>&1 | head -20

# 2. 确认开发服务器无报错（如果正在运行）
```

#### 后端修改后
```bash
# 1. 检查语法错误
cd i:\AIGameTest\SilentWerewolf\server && node --check index.js

# 2. 如有服务在运行，重启验证
```

#### 同时修改前后端
两边都要验证。

## 调试规范

### 遇到错误时
1. **先读错误信息** — 完整阅读报错，不要猜测
2. **定位根因** — 搜索代码找到出错位置
3. **理解逻辑** — 读相关代码理解为什么出错
4. **精确修复** — 修最小范围
5. **验证修复** — 确认错误消失且没引入新问题

### 禁止的调试方式
- ❌ 不读错误信息就瞎改
- ❌ 大面积重写来"修"一个小 bug
- ❌ 改了不验证就说"应该好了"
- ❌ 引入临时 workaround 而不修根因

## 启动服务命令

### 后端
```bash
cd i:\AIGameTest\SilentWerewolf\server && node index.js
```

### 前端
```bash
cd i:\AIGameTest\SilentWerewolf\client && npx vite
```

### 同时启动（开发模式）
后端和前端需要在不同终端分别启动。

## 文件变更后的通知
完成所有修改后，必须向用户清晰列出：
1. 修改了哪些文件
2. 每个文件改了什么
3. 如何验证（如需要用户手动测试）

**规则优先级**：🔴 高优先级